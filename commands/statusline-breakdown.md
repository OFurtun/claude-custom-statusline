Toggle the statusline context breakdown display.

Check the current value of `show_breakdown` in `~/.claude/statusline.config.json` and toggle it (if true, set to false; if false, set to true).

After toggling, tell the user whether the breakdown is now enabled or disabled.
