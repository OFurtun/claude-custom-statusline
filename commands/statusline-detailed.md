Toggle the statusline detailed display.

Check the current value of `show_detailed` in `~/.claude/statusline.config.json` and toggle it (if true, set to false; if false, set to true).

After toggling, tell the user whether the detailed view is now enabled or disabled.
